def check_password():
    min_length = 8  # You can change this value if needed
    password = input("Enter your password: ")

    if len(password) >= min_length:
        print("Password is valid ✅")
    else:
        print(f"Password is too short ❌ (Minimum {min_length} characters required)")

# Run the function
check_password()
